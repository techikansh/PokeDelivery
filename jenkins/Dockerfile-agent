FROM jenkins/inbound-agent:latest-jdk17

USER root

RUN apt-get update && apt-get install -y \
    curl \
    jq \
    unzip \
    && rm -rf /var/lib/apt/lists/*

# any other tools the agent needs for artifact download
# e.g. gh CLI, unzip for release assets, etc.

USER jenkins